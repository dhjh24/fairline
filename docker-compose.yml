services:
  fairline:
    build: .
    image: fairline:latest
    ports:
      - "${PORT:-8080}:8080"
    environment:
      XAI_API_KEY: ${XAI_API_KEY:-}
      HOST: 0.0.0.0
      PORT: "8080"
      NITRO_HOST: 0.0.0.0
      NITRO_PORT: "8080"
    restart: unless-stopped
